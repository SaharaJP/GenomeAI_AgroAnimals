# T13-01 — Step 3

Что добавлено в этом инкременте:
- richer artifact discovery для jobs: не только key=value из логов, но и автопоиск по артефактным директориям run/ref;
- JSON API для log tail и списка артефактов job;
- preview для текстовых/JSON/CSV/HTML/YAML артефактов из карточки job;
- e2e smoke/tests для полного цикла report job + cancel/retry очереди.

Покрытие smoke/tests:
- `tests/web/test_t13_01_job_runner_step1.py`
- `tests/web/test_t13_01_job_runner_step2.py`
- `tests/web/test_t13_01_job_runner_step3.py`

Ограничение шага:
- preview пока текстовый, без inline PDF/docx viewer;
- live-stream логов не реализован, используется tail/raw API;
- финальный следующий шаг — polishing UI очереди и, при необходимости, SSE/websocket stream для running jobs.
