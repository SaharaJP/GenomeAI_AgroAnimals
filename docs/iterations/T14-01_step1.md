# T14-01 — шаг 1

Что сделано:
- введён нормализованный `copilot_fact_pack` (`genomeai.copilot.fact_pack.v1`);
- fact-pack строится из текущего assistant fact-pack без переписывания existing MVP+ flow;
- в pack выделяются `facts`, `tables`, `sources`, `missing_data_requests`;
- AI assistant теперь сначала строит чанки из `copilot_fact_pack`;
- fallback-ответ сообщает, каких данных не хватает и как их получить.

Проверка:
- `pytest -q tests/test_t14_01_copilot_fact_pack_step1.py tests/test_t8_02_ai_assistant_rag.py`

Ограничения этого шага:
- LLM-путь ещё не переведён на обязательный шаблон с inline-цитатами по каждому числу;
- строгая блокировка ответов без цитат будет усилена на следующих шагах T14-01.
