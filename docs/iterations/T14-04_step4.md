# T14-04 step 4

Что закрыто в этой итерации:
- explainability расширена с mastitis risk на productivity ML (train/score);
- train сохраняет explainability_profile.json для regression-модели;
- score пишет explain_top_factors_text / explain_counterfactuals_text в scored_latest.csv и productivity_explanations.csv;
- explainability встроена в report / regular_reports / Animal Profile;
- Copilot может объяснять `почему` по productivity-таблицам, т.к. explainability-поля попадают в fact-pack preview rows.

Ограничения:
- контрфакты остаются one-feature simulation на обученной модели;
- это decision-support, а не causal inference.
