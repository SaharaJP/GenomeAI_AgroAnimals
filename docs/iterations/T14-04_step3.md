# T14-04 — step 3

## Что сделано
- Добавлен структурированный `animal_explainability` блок в fact-pack/report для mastitis-risk.
- Copilot `why` теперь лучше отвечает по `animal_id=...`, выбирая explainability-строку конкретного животного.
- В `Animal Profile` добавлен read-only блок explainability по последнему scoring run.
- Добавлен audit best-effort на просмотр explainability в профиле животного.

## Ограничения
- Контрфакты остаются one-feature simulation, не causal.
- Для UI берётся latest scoring run по `data_version`.
- Если в latest scoring run нет строки по animal_id, показывается NA/fallback.
