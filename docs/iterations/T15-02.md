# T15-02 — core package + shim layer (без крупного переноса логики)

## Что сделано

- Добавлен новый пакет `src/core/{domain,application,infra}`.
- В `core.domain` вынесены dataclass-модели для `verify_refactor`.
- В `core.application` перенесён orchestration/use-case слой `verify_refactor`.
- В `core.infra` вынесены service-adapter и helper деprecation-warning.
- Старые пути `src/genomeai/application/*` превращены в shim-модули: они проксируют импорт в `core.*` и дают `DeprecationWarning`.
- `src/genomeai/cli.py` теперь как thin-adapter импортирует use-case напрямую из `core.application`.
- `src/genomeai/refactor_verify.py` сохраняет backward-compatible low-level facade, но использует compare/runtime helpers из `core.application`.

## Что НЕ делалось

- Не переносилась крупная low-level логика snapshot/build/update из `src/genomeai/refactor_verify.py`.
- Не менялись CLI-команды, Streamlit pages, web routes и артефактные контракты.

## Проверка

- `pytest -q tests/test_t15_01_verify_refactor.py tests/test_t15_02_core_shims.py`
- `python -m genomeai verify_refactor --scenarios standard`
- `python -m genomeai smoke --out-version <dv> --artifacts <artifacts_root>`
- полный `pytest -q`

## Известное ограничение регрессий

- Расширенный grouped pytest выявил не связанный с T15-02 pre-existing сбой web/job-runner ветки: `sqlite3.IntegrityError: audit_log is append-only; only archive mark is allowed` в `web_cabinet/db.py:init_db()`.
- Сбой воспроизводится и на pristine архиве T15-01 (`tests/web/test_t13_01_job_runner_step3.py::test_cancel_and_retry_flow_visible_in_queue`), поэтому в T15-02 не исправлялся как отдельная поведенческая задача.
