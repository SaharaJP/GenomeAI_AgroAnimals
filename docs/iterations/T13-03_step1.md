# T13-03 / Шаг 1 — Базовый каталог data contracts

Что сделано:
- добавлен `configs/contracts/catalog.json` как машиночитаемый каталог схем;
- добавлен `src/genomeai/contracts_catalog.py` для сборки единого manifest JSON;
- добавлен CLI `genomeai contracts-catalog`;
- ingest summary теперь сохраняет `contract_version` в lineage/metadata;
- добавлены smoke/unit тесты для каталога.

Проверка:
```bash
pytest -q tests/test_t13_03_contract_catalog_step1.py
python -m genomeai.cli contracts-catalog --contracts configs/contracts --catalog configs/contracts/catalog.json --output artifacts/system/data_contract_catalog.json
```

Следующий шаг:
- усилить валидаторы импорта по версиям контрактов и подготовить reusable mapping templates для СЕЛЭКС/1С/Excel.
