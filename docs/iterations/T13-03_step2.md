# T13-03 — step 2

## Что сделано

- Добавлен offline-core модуль `contract_precheck.py` для pre-ingest валидации исходного файла по `contract + mapping`.
- Web upload теперь валидирует файл до `create_job`; invalid datasets не ставятся в очередь.
- В `audit_log` пишется `contract.validate` с `status=OK/FAIL`, `data_version`, preview ошибок и путями к source/mapping.
- Connector runner валидирует binding до ingest и сохраняет JSON-отчет с деталями ошибки в артефакты run.
- Добавлены reusable mapping templates для `selex`, `1c`, `excel` по `farms/animals/lactations`.
- Каталог контрактов научился автоматически видеть шаблоны в `configs/mappings/templates/**`.

## Smoke / tests

```bash
PYTHONPATH=src pytest -q \
  tests/test_t13_03_contract_validation_step2.py \
  tests/web/test_t13_03_contract_validation_web_step2.py \
  tests/test_a1_ingest.py \
  tests/web/test_t13_02_connectors_step3_upload.py
```

## Ограничения шага

- UI каталога схем и отдельная страница просмотра mapping templates еще не добавлены в этом шаге.
- Validation пока фокусируется на `required/source columns`, `type coercion`, `allowed_values`, `empty required values`; более глубокие междатасетные checks остаются в QC.
