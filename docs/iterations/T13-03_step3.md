# T13-03 — step 3

## Что сделано

- Добавлен UI Data Catalog 2.0: `/contracts` и JSON API `/api/contracts/catalog`.
- Каталог показывает версии контрактов, required fields, source systems, examples, QC coverage и reusable mapping templates.
- Добавлены фильтры по `domain`, `status`, `source system` и текстовый поиск по dataset/описанию/полям.
- Каталог контрактов теперь умеет выгружаться не только в JSON, но и в Markdown (`docs/contracts/catalog.md`).
- Навигация web-cabinet дополнена ссылкой `Data Catalog`; на странице upload добавлена ссылка на каталог контрактов.

## Smoke / tests

```bash
PYTHONPATH=src pytest -q \
  tests/test_t13_03_contract_catalog_step1.py \
  tests/test_t13_03_contract_catalog_step3.py \
  tests/test_t13_03_contract_validation_step2.py \
  tests/web/test_t13_03_contract_catalog_web_step3.py \
  tests/web/test_t13_03_contract_validation_web_step2.py \
  tests/test_a1_ingest.py \
  tests/web/test_t13_02_connectors_step3_upload.py
```

## Ограничения шага

- Каталог пока читает схемы и шаблоны из файловой системы проекта; редактирование контрактов из UI в этом шаге не добавлялось.
- QC coverage в каталоге остается декларативным мета-слоем поверх уже реализованных `ingest/qc` проверок и не заменяет выполнение QC-run.
