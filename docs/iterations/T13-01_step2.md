# T13-01 — Step 2

Что добавлено в этом инкременте:
- config-driven auto-retry для job runner с backoff;
- фильтры очереди jobs в API и HTML UI;
- отображение retry_source / next_attempt_at / семейства попыток;
- audit-запись `pipeline.auto_retry_scheduled` при автоматическом ретрае.

Покрытие smoke/tests:
- `tests/web/test_t13_01_job_runner_step1.py`
- `tests/web/test_t13_01_job_runner_step2.py`

Ограничение шага:
- авто-ретрай пока ориентирован на exit code и kind job;
- ещё нет live-stream логов и продвинутого browser для артефактов;
- финальный e2e сценарий enqueue → run → cancel/retry → artifacts будет собран отдельным шагом.
