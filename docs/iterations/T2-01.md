# T2-01 — Alert Center v2

## Цели
... 

## RBAC
...

## Lifecycle
new → acknowledged → resolved (+ reason)

## Источники генерации
- QC v2: `artifacts/qc2/<data_version>/<qc_run>/alerts_auto.csv`
- ML/факты: `artifacts/canonical/<data_version>/dm_alerts.csv`
- Бизнес-правила: `dm_treatments.csv` (withdrawal), `dm_repro.csv` (если есть), `dm_sensor_readings.csv` (если есть)

## Версионирование
Заполняются поля: data_version, qc_run, model_version, scoring_run, report_version.
