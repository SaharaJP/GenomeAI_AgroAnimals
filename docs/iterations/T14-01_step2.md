# T14-01 — шаг 2

## Что сделано
- Добавлен конфиг `configs/copilot/copilot_v2.yaml` с дефолтным строгим режимом `strict_source_only=true`.
- Copilot теперь формирует детерминированный ответ только из `copilot_fact_pack`, даже если вызов пришёл с `use_llm=True`.
- Для подтверждённых фактов и таблиц добавлены inline-цитаты: `fact_id/section/table/metric/run_id/report_version`.
- Для нехватки данных оставлен отдельный шаблон ответа с блоками `Нужны данные` и `Как получить`.
- Добавлены тесты на inline-цитирование, строгий source-only режим и отсутствие unsupported answer при пустом fact-pack.

## Что не покрыто этим шагом
- LLM ещё не переведён на fully constrained JSON-output с пост-валидацией каждой цитаты; на этом шаге он просто отключается в strict mode.
- UI-кнопки для просмотра inline-citation targets и deep links в конкретные таблицы/run pages будут добавлены следующим подшагом.

## Smoke
```bash
pytest -q tests/test_t14_01_copilot_fact_pack_step1.py tests/test_t14_01_copilot_fact_pack_step2.py tests/test_t8_02_ai_assistant_rag.py
```
