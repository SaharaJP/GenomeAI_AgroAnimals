import { NextRequest, NextResponse } from 'next/server'; import { backendFetch,clearAuthCookies,getAuthTokens } from '@/lib/server/backend';
export async function POST(request:NextRequest){const payload=await request.json().catch(()=>({}));const {accessToken}=await getAuthTokens(); if(accessToken){await backendFetch('/api/app/v1/auth/logout',{method:'POST',accessToken,headers:{'content-type':'application/json'},body:JSON.stringify({all_devices:Boolean(payload.all_devices)})}).catch(()=>null)} const response=NextResponse.json({ok:true}); clearAuthCookies(response); return response}
