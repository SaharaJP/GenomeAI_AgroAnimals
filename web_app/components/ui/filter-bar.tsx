'use client'; import { useState } from 'react';
export function FilterBar({placeholder='Filter…',onChange}:{placeholder?:string;onChange?:(value:string)=>void}){const [value,setValue]=useState('');return <div className="card toolbar"><input className="input" value={value} placeholder={placeholder} onChange={(e:any)=>{const next=e.target.value;setValue(next);onChange?.(next)}}/><select className="input" defaultValue="all"><option value="all">All farms</option><option value="active">Active scope</option></select><select className="input" defaultValue="today"><option value="today">Today</option><option value="week">This week</option><option value="month">This month</option></select></div>}
