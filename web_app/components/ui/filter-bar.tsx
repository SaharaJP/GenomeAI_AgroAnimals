'use client'; import { useState } from 'react';
export function FilterBar({placeholder='Фильтр…',onChange}:{placeholder?:string;onChange?:(value:string)=>void}){const [value,setValue]=useState('');return <div className="card toolbar"><input className="input" value={value} placeholder={placeholder} onChange={(e:any)=>{const next=e.target.value;setValue(next);onChange?.(next)}}/><select className="input" defaultValue="all"><option value="all">Все фермы</option><option value="active">Активный scope</option></select><select className="input" defaultValue="today"><option value="today">Сегодня</option><option value="week">Эта неделя</option><option value="month">Этот месяц</option></select></div>}
