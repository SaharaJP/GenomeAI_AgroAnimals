import Link from 'next/link'; import { Badge } from '@/components/ui/badge'; import type { WorklistItem } from '@/lib/api/contracts';
export function WorklistList({items}:{items:WorklistItem[]}){return <div className="grid">{items.map(item=><section className="card" key={item.task_id}><div className="toolbar" style={{justifyContent:'space-between'}}><div><h3 className="card-title">{item.title}</h3><p className="card-subtitle">{item.task_type} · priority {item.priority} · farm {item.entity?.farm_id||'—'}</p></div><Badge tone={item.is_overdue?'danger':item.status==='done'?'success':'warning'}>{item.status}</Badge></div>{item.why&&Object.keys(item.why).length>0?<ul className="bullet-list compact">{Object.entries(item.why).slice(0,3).map(([key,value])=><li key={key}>{key}: {String(value)}</li>)}</ul>:null}<div className="linked-inline-actions"><Link href={item.entity?`/profiles/${item.entity.object_type}/${item.entity.object_id}`:'#'}>Profile</Link><Link href={`/copilot?target=worklists&task_id=${encodeURIComponent(item.task_id)}`}>Explain</Link><Link href={`/decisions?task_id=${encodeURIComponent(item.task_id)}`}>Decision hook</Link><Link href={`/support?task_id=${encodeURIComponent(item.task_id)}`}>Feedback hook</Link></div></section>)}</div>}
